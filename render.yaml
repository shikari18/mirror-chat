services:
  - type: web
    name: zuri-mirror-chat
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: VITE_OPENROUTER_API_KEY
        sync: false
